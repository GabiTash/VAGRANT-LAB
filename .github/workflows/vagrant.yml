jobs:
  update-vagrant:
    runs-on: self-hosted

    defaults:
      run:
        working-directory: vagrant-dev-lab

    steps:
      - name: Pull latest code
        run: git pull

      - name: Reload Vagrant
        run: vagrant reload --provision
